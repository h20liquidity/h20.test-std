[profile.default]
src = "src"
out = "out"
libs = ["lib"]

solc = "0.8.25"

# These settings should be used for snapshots
optimizer = true
optimizer_runs = 1000000
evm_version = "cancun"
bytecode_hash = "none"
cbor_metadata = false

# Needed for dotrain compose
ffi = true

[fuzz]
runs = 1024